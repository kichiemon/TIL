# Kitura

# how to getting start

```
g push
g commit -m"Say 'Hello World'"
g add .
g st
g push -u origin master
g commit -m"Initial Commit"
vim .gitignore
open try-kitura.xcodeproj
ll
cp ../../iKichiemon/TKMessagingKit/.gitignore ./
git push -u origin master
git remote add origin git@github.com:kichiemon/try-kitura.git
git init
kitura init
rm -rf .git
ls -la
rm -rf ./*
mkdir backend
kitura init .
kitura init
which kitura
cd -
cd /usr/local/etc/bash_completion.d
sh /usr/local/etc/bash_completion.d
/usr/local/etc/bash_completion.d
brew install kitura
brew tap ibm-swift/kitura
ghq get git@github.com:kichiemon/try-kitura.git
swift test
ghq get git@github.com:IBM-Swift/Kitura.git
```
